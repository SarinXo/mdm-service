mdm-service:
  datasource:
    url: jdbc:postgresql://localhost:5454/mdm-service
    username: test
    password: pswd
    schema: mdm

spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true